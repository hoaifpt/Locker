$kind: http-request
url: "{{base_url}}/api/orders/:id"
method: GET
headers:
  Authorization: Bearer {{token}}
pathVariables:
  id: "1"
auth:
  type: bearer
  credentials:
    token: "{{token}}"
order: 1000
