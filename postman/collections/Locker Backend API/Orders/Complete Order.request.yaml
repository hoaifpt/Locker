$kind: http-request
url: "{{base_url}}/api/orders/:id/complete"
method: PATCH
headers:
  Authorization: Bearer {{token}}
pathVariables:
  id: "1"
auth:
  type: bearer
  credentials:
    token: "{{token}}"
order: 7000
