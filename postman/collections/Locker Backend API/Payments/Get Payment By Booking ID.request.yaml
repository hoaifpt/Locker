$kind: http-request
url: "{{base_url}}/api/payments/booking/:bookingId"
method: GET
headers:
  Authorization: Bearer {{token}}
pathVariables:
  bookingId: "1"
auth:
  type: bearer
  credentials:
    token: "{{token}}"
order: 2000
