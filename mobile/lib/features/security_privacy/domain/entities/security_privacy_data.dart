class SecurityPrivacyData {
  final bool faceIdEnabled;
  final bool twoFactorEnabled;
  final bool loginAlertEnabled;

  const SecurityPrivacyData({
    required this.faceIdEnabled,
    required this.twoFactorEnabled,
    required this.loginAlertEnabled,
  });
}
