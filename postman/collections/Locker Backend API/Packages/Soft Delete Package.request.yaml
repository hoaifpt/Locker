$kind: http-request
url: "{{base_url}}/api/packages/:id/soft-delete"
method: PUT
headers:
  Authorization: Bearer {{token}}
pathVariables:
  id: "1"
auth:
  type: bearer
  credentials:
    token: "{{token}}"
order: 5000
