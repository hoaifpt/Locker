$kind: http-request
url: "{{base_url}}/api/orders/:id/confirm"
method: PATCH
headers:
  Authorization: Bearer {{token}}
pathVariables:
  id: "1"
auth:
  type: bearer
  credentials:
    token: "{{token}}"
order: 4000
